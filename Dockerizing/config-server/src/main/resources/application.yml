server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:raulcfr112/config-server
          default-label: main
          search-paths:
            - 'greeting-service*'